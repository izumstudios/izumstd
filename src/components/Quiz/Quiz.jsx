import React from 'react'
import Marquiz from '../UI/Marquiz/Marquiz'
import styles from './Quiz.module.scss'

const Quiz = () => {
	return (
		<div className={styles.container}>
			<Marquiz />
		</div>
	)
}

export default Quiz
